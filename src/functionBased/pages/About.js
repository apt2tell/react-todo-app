import React from 'react';

const About = () => (
  <div>
    <p className="about">
      React Todo App
      This is a simple Todo App built with React components.
      Allows user to mark/unmark, add Todo,
      delete and update Todo tasks and persist data in local storage.
    </p>
  </div>
);
export default About;
